import InvitationChrome from "@/components/templates/InvitationChrome";

export default function WorkLiveLayout({ children }: { children: React.ReactNode }) {
  return <InvitationChrome>{children}</InvitationChrome>;
}
